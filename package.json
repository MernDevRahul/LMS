{
  "name": "techstach-lms",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "", 
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix google_meet && npm run build --prefix frontend",
    "start": "npm run start --prefix backend && npm run start --prefix google_meet"
  }
}
